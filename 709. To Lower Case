class Solution {
public:
    string toLowerCase(string str) {
        string result;
        for(int i = 0; i < str.length(); ++i){
            result += tolower(str[i]);
        }
        return result;
    }
};
